<!-- Generated by `splunkctl docs generate` — do not edit by hand. -->

# splunkctl auth

Browser SAML login, session status, and logout for SIEM and SOAR.

## auth login

Complete SAML/MFA in a browser and store a validated product session.

```text
splunkctl auth login [OPTIONS]
```

**Flags**

| Flag | Type | Default | Description |
|---|---|---|---|
| `--target` | choice: siem, soar | - | [required] |

## auth logout

Best-effort remote logout, then guaranteed local session removal.

```text
splunkctl auth logout [OPTIONS]
```

**Flags**

| Flag | Type | Default | Description |
|---|---|---|---|
| `--target` | choice: siem, soar, both | - |  |

## auth status

Report browser-session status: missing, valid, expired, or unreachable.

```text
splunkctl auth status [OPTIONS]
```

**Flags**

| Flag | Type | Default | Description |
|---|---|---|---|
| `--target` | choice: siem, soar, both | - |  |
