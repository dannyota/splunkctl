<!-- Generated by `splunkctl docs generate` — do not edit by hand. -->

# splunkctl config

Manage splunkctl configuration.

## config init

Interactive setup — create or overwrite config.

```text
splunkctl config init [OPTIONS]
```

```text
Bare ``config init`` writes the flat (legacy-compatible) file — unless
the destination already exists and uses the ``profiles:`` schema, in
which case it folds the new values into ``profiles.default`` instead,
leaving sibling profiles and the ``current`` pointer untouched. This
never clobbers an existing multi-profile file.
``config init --profile <name>`` targets that named profile instead —
use ``config use <name>`` afterwards to make it active.

``config init --soar`` prompts for SOAR connection settings and saves
them as a nested ``soar:`` map within the profile, preserving existing
SIEM fields.
```

**Flags**

| Flag | Type | Default | Description |
|---|---|---|---|
| `--host` | text | - | Splunk host. |
| `--port` | integer | - | Splunk port. |
| `--username` | text | - | Splunk username. |
| `--password` | text | - | Splunk password. |
| `--scheme` | choice: https, http | - | Connection scheme. |
| `--verify / --no-verify` | flag | - | Verify SSL certificate. |
| `--soar` | flag | false | Add SOAR connection settings to the profile. |
| `--profile` | text | - | Create or update this named profile (schema v2), instead of the flat/default config file. A legacy file is upgraded, folding its existing values into 'profiles.default'. |
| `--path` | path | - | Config file path (default: ~/.splunkctl/config.yaml). |
| `--web-url` | text | - | Product web origin (Splunk Web, e.g. http://host:8000). Required for browser SAML login on SIEM. |
| `--auth-mode` | choice: auto, token, password, browser | auto | Authentication mode. 'auto' probes the login route to detect SAML. |

## config show

Display config (secrets redacted).

```text
splunkctl config show [OPTIONS]
```

```text
Shows the active profile (``--profile`` flag > ``current:`` pointer >
``default``) plus a one-line list of other known profiles. Pass the
global ``--profile <name>`` to show a specific profile instead.
```

## config test

Verify connectivity and auth against the Splunk instance.

```text
splunkctl config test [OPTIONS]
```

## config use

Switch the active profile — sets 'current', no connectivity test.

```text
splunkctl config use [OPTIONS] NAME
```
