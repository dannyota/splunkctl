# splunkctl — Agent Operating Guide

> Placeholder. This will be the embedded skill guide for Claude Code.
> Run `splunkctl skill` to print this guide.
> Run `splunkctl skill install` to write it to `~/.claude/skills/splunkctl/SKILL.md`.
